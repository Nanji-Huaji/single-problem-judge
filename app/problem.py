PROBLEM = {
    "slug": "snake",
    "title": "交互式贪吃蛇游戏",
    "statement": """
## 题目描述
编写一个交互式程序来控制一条在 20×20 判断网格中的贪吃蛇（由于是交互式，请在每次输出后要求调用 `fflush(stdout)` 或等效函数刷新缓冲区）。
蛇身初始长度为 3（含蛇头），默认方向为向上。
游戏中共有 10 个随机刷新的不可移动障碍物。

### 地图元素
- `#`：墙壁，不可穿越。
- `.`：空地，可移动区域。
- `H`：蛇头，所在位置。
- `B`：蛇身。
- `F`：食物，蛇吃掉可增长 1 节长度。
- `O`：固定障碍物，碰撞即死。

### 规则
1. **移动**：输入 `W`/`A`/`S`/`D` 表示 上、左、下、右（不得直接反向）。
2. **得分**：初始 0 分，吃到食物 +10 分。
3. **增长**：每次吃到食物长度 +1；另外用户**每走满 N 步**长度 +1（若恰巧走满 N 步时又吃到了食物，仅加 1 节算增长）。
4. **游戏结束**：撞墙、撞障碍、撞蛇身即结束。
""",
    "input_format": """
1. 游戏启动后，评测机首先给出 20 行长度为 20 的字符串，这是初始二维地图。
2. 第 21 行给出正整数 N (每走 N 步涨一节)。
3. 在每次玩家输出移动后，交互程序将给出一行 2 个以空格分隔的由评测机返回的坐标：
   - 包含 `row col`（两个均大于 0 小于 19 的整数）：当前操作吃到了食物，返回了新出生的食物坐标。
   - `20 20`：表示交互正常，请玩家继续。
   - `100 100`：表示游戏结束（触碰死亡边界）。
""",
    "output_format": """
1. 正常交互时，每次输出两行：
   - 第 1 行：移动方向字符 (`W`/`A`/`S`/`D`)
   - 第 2 行：移动前的得分整数。
2. 如果收到交互结束指令 `100 100`，表示你的蛇死亡，你必须紧接着输出当前的地图状态及最终得分。
   - 前 20 行：此时的 20×20 字符地图。
   - 第 21 行：最终分数整数。
程序最后随即结束（Return 0）。
""",
    "sample_input": """(作为交互程序输出的样例)
####################
#..................#
#.................F#
#......H...........#
#......B...........#
#......B...........#
#..................#
####################
32
20 20
20 20
100 100
""",
    "sample_output": """(作为玩家程序输出的样例)
W
0
W
0
D
0
... (输出结束地图和分数)
""",
    "time_limit_ms": 2000,
    "memory_limit_mb": 128,
    "supported_languages": ["c", "cpp17", "python3"],
    "tests": [
        {"n": 1, "seed": 101},
        {"n": 2, "seed": 102},
        {"n": 4, "seed": 103},
        {"n": 8, "seed": 104},
        {"n": 16, "seed": 105},
        {"n": 32, "seed": 106},
        {"n": 64, "seed": 107},
        {"n": 128, "seed": 108},
        {"n": 256, "seed": 109},
        {"n": 512, "seed": 110},
    ],
}
